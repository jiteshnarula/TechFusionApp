<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jiteshnarula.techfusionapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppCommonContext"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/SplashAndStart">
        <activity android:name=".splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AttendedWorkshop.AttendedWorkshopActivity" />
        <activity android:name=".Videos.VideosActivity" />
        <activity android:name=".More.MoreActivity" />
        <activity android:name=".Login.SignInActivity"/>
        <activity android:name=".Home.HomeActivity" />
        <activity android:name=".Workshop.WorkshopActivity" />
        <activity android:name=".Login.SignUpActivity"/>
        <activity android:name=".splash.StartActivity" />
        <activity android:name=".Login.ForgotPasswordActivity"/>
    </application>

</manifest>